<h1>Ajouter une dose à la recette du <%= @cocktail.name %></h1>

<%= simple_form_for [ @cocktail, @dose ] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient %>
  <%= f.submit %>
<% end %>

<%= link_to 'Annuler', cocktail_path(@cocktail) %>
